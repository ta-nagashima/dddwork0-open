package example.service

import spock.lang.Specification

class InputAttendanceServiceSpec extends Specification {
    def "勤怠登録service動作確認"() {
        setup:
        def inputAttendanceRepository = Mock(inputAttendanceRepository)


        when:

        then:

        where:
        x   |   y  ||  z
    }
}
